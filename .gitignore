# =============================================================================
# This repository is organized into 4 zones:
#
#   /services/    — 11 Go system-design services (code)
#   /pkg/         — 2 shared Go packages (code)
#   /book/        — GitBook documentation (docs)
#   /assets/      — Brand assets: logo, banner (media)
#   /.github/     — CI, templates, community files (meta)
#
#   go.mod, go.sum — Go module files
#   README.md      — Project landing page
#   LICENSE        — MIT
# =============================================================================

# Superpowers internal docs (auto-generated plans/specs)
docs/

# Environment files — .env.example is committed as template
.env
.env.*
!.env.example
_book/

# Compiled Go binaries (root only)
/flash-sale
/url-shortener
/rate-limiter
/chat-system
/notification-system
/unique-id-generator
/key-value-store
/search-autocomplete
/news-feed
/web-crawler
/youtube
/google-drive
